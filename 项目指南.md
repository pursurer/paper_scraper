## 项目的行动指南

###  🌐 宏观项目蓝图 (Global Context)

> **说明**：此部分定义项目的终极目标和整体进度。

* **Project Goal**: 创建 `paper_scraper` - 一个统一的顶会论文获取工具，整合三种数据来源（OpenReview API、网页爬取、PDF 提取），提供标准化的 CLI 入口和统一的 CSV 输出格式。

* **Master Plan (High-level Breakdown)**:
  - [x] 阶段一：核心架构搭建 ✅
  - [x] 阶段二：核心模块迁移 ✅
  - [x] 阶段三：网页爬取模块 ✅
  - [x] 阶段四：PDF 提取模块 ✅
  - [x] 阶段五：CLI 工具开发 ✅
  - [x] 阶段六：配置外部化 ✅
  - [ ] 阶段七：清理与文档 - 完善 README 和使用文档

---

###  📜 TDD 演进日志 (Implementation History)

| 阶段 | 描述 | Commit | 测试数 |
|------|------|--------|--------|
| 阶段一 | 核心架构搭建 | `27a08e4` | 4 |
| 阶段二 | 核心模块迁移 | `ea47dae` | 171 |
| 阶段三 | 网页爬取模块 | `b708b6e` | 42 |
| 阶段四 | PDF 提取模块 | `f9f388d` | 28 |
| 阶段五 | CLI 工具 | `c2bd1f4` | 26 |
| 阶段六 | 配置外部化 | `836e5b8` | 22 |
| **总计** | **11 个模块** | | **293 个测试** |

---

## 当前项目结构

```
论文获取/
├── paper_scraper/              # ✅ 核心 Python 包
│   ├── __init__.py             # ✅ 包入口 (v0.1.0)
│   ├── __main__.py             # ✅ CLI 入口
│   ├── utils.py                # ✅ 工具函数
│   ├── extractor.py            # ✅ 字段提取器
│   ├── filters.py              # ✅ 关键词过滤器
│   ├── venue.py                # ✅ Venue 发现与分组
│   ├── paper.py                # ✅ 论文获取
│   ├── scraper.py              # ✅ Scraper 主类
│   ├── web_scraper.py          # ✅ 网页爬取
│   └── pdf_extractor.py        # ✅ PDF 提取
│
├── config/                     # ✅ 配置目录
│   ├── __init__.py             # ✅ Config 类
│   └── config.example.py       # ✅ 配置模板
│
├── tests/                      # ✅ 测试目录 (293 个测试)
│   ├── test_package.py
│   ├── test_utils.py
│   ├── test_extractor.py
│   ├── test_filters.py
│   ├── test_venue.py
│   ├── test_paper.py
│   ├── test_scraper.py
│   ├── test_web_scraper.py
│   ├── test_pdf_extractor.py
│   ├── test_cli.py
│   └── test_config.py          # ✅ 新增
│
└── ...
```

## 数据来源映射

| 会议 | 来源类型 | 状态 |
|------|---------|------|
| ICLR, ICML, NeurIPS | OpenReview | ✅ |
| AAAI, IJCAI, ACL, EMNLP, NAACL, AISTATS | Web Scrape | ✅ |
| AAMAS | PDF Extract | ✅ |

## 配置系统

### 方式一：环境变量
```bash
export OPENREVIEW_EMAIL="your_email@example.com"
export OPENREVIEW_PASSWORD="your_password"
export PAPER_SCRAPER_DELAY_MIN="2.0"
export PAPER_SCRAPER_DELAY_MAX="5.0"
export PAPER_SCRAPER_TIMEOUT="30"
```

### 方式二：配置文件
```bash
cp config/config.example.py config/config.py
# 编辑 config/config.py 填入凭证
```

### 方式三：Python API
```python
from config import get_config, Config

config = get_config()
print(config.openreview_email)
print(config.request_timeout)
```

## 使用示例

### 命令行 (CLI)
```bash
# 列出支持的会议
python -m paper_scraper --list-conferences

# 爬取 IJCAI 2024
python -m paper_scraper -c IJCAI -y 2024 -o ijcai_2024.csv

# 批量爬取
python -m paper_scraper -c ICLR ICML -y 2023 2024 --output-dir ./output

# PDF 提取
python -m paper_scraper --pdf-dir ./aamas2025 -y 2025 -o aamas.csv
```

### Python API
```python
from paper_scraper import Scraper, Extractor, scrape_ijcai, extract_aamas_metadata

# OpenReview 来源
scraper = Scraper(conferences=['ICLR'], years=['2024'], ...)
scraper()

# 网页爬取
papers = scrape_ijcai(2024, output_path='ijcai_2024.csv')

# PDF 提取
papers = extract_aamas_metadata('./aamas2025/', 2025)
```

---

## 🔜 下一步：阶段七

**清理与文档**：
- 完善 README.md
- 更新 AGENTS.md
- 整理冗余文件
